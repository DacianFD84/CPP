#include <stdio.h>

int main() {
    // Declare three variables
    int a = 5;
    int b = 10;
    int c = 15;

    // Print the values of the variables and their sum
    printf("The values are a = %d, b = %d, c = %d\n", a, b, c);


    return 0;
}
