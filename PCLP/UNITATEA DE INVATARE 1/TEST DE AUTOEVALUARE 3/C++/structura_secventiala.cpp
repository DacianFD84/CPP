#include <iostream>

int main() {
    int a = 5;        // Inițializare variabilă
    int b = 10;       // Inițializare variabilă
    int suma = a + b; // Calculăm suma

    std::cout << "Suma este: " << suma << std::endl;  // Afișăm rezultatul
    system("pause");
    return 0;
}
