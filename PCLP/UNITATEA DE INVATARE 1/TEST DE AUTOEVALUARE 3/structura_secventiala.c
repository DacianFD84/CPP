#include <stdio.h>

int main() {
    int a = 5, b = 10, suma;

    // Structură secvențială: operațiile sunt executate în ordine
    suma = a + b;
    printf("Suma dintre %d si %d este: %d\n", a, b, suma);

    return 0;
}
